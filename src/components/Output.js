import React from 'react';
import { Card, Typography } from '@mui/material';

const Output = () => {
  return (
    <Card sx={{ padding: 2 }}>
      <Typography variant="h6">Output</Typography>
      <Typography variant="body2">SAP2000 Disconnected</Typography>
      <Typography variant="body2">Set coordinates for plates, output 1.0</Typography>
      <Typography variant="body2">Set coordinates for plates, output 1.0</Typography>
    </Card>
  );
};

export default Output;
